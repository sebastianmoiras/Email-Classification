# Email Classification – Spam Detection with BERT

## Project Description
This project implements an email classification model to detect **spam** vs **ham (not spam)** using **BERT (Bidirectional Encoder Representations from Transformers)**.  
Why BERT? BERT provides contextual embeddings that significantly improve classification performance compared to traditional methods.  
